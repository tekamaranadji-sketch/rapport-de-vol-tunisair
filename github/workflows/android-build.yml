name: Build React Native Android APKs

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout du code
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: 💻 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 3️⃣ Installer les dépendances Node.js (tolère l'absence de fichier lock)
      - name: 📦 Install Node.js dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          elif [ -f yarn.lock ]; then
            yarn install
          else
            echo "Pas de fichier lock trouvé, installation npm par défaut"
            npm install
          fi

      # 4️⃣ Setup JDK 17 pour Android
      - name: ☕ Setup JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # 5️⃣ Build APK Debug
      - name: 🏗️ Build Debug APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew clean
          ./gradlew assembleDebug --no-daemon --stacktrace

      # 6️⃣ Upload Debug APK
      - name: 📦 Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: Rapport_de_vol_Tunisair-Debug-${{ github.run_number }}-${{ github.sha }}
          path: android/app/build/outputs/apk/debug/app-debug.apk

      # 7️⃣ Setup keystore pour Release (optionnel)
      - name: 🔑 Setup Keystore
        if: secrets.KEYSTORE_BASE64
        run: |
          mkdir -p android/app
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/my-release-key.jks

      # 8️⃣ Build APK Release signé (optionnel)
      - name: 🏗️ Build Release APK
        if: secrets.KEYSTORE_BASE64
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleRelease -Pandroid.injected.signing.store.file=app/my-release-key.jks \
                                    -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
                                    -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
                                    -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }} \
                                    --no-daemon --stacktrace

      # 9️⃣ Upload Release APK (optionnel)
      - name: 📦 Upload Release APK
        if: secrets.KEYSTORE_BASE64
        uses: actions/upload-artifact@v4
        with:
          name: Rapport_de_vol_Tunisair-Release-${{ github.run_number }}-${{ github.sha }}
          path: android/app/build/outputs/apk/release/app-release.apk
